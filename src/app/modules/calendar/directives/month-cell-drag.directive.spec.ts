import { MonthCellDragDirective } from './month-cell-drag.directive';

describe('MonthCellDragDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthCellDragDirective();
    expect(directive).toBeTruthy();
  });
});
